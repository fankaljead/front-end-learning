function foo(input: string): string {
  return input;
}

foo("zxh");

window.addEventListener("load", (e) => {});

let yf = "sss";
yf.replaceAll("s", "ss");

type Fxo = {
  name: string;
  age: number;
};

type Bxr = {
  age: number;
  address: string;
};

type FB = Fxo & Bxr;

declare const fb: FB;
